# Eval Layout

- `eval/metadata`: manifests, run configs, and provenance metadata
- `eval/data`: local dataset pointers/snapshots used for evaluation
- `eval/results`: outputs from reproduction/evaluation runs
